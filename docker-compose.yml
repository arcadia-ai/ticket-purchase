services:
  ticket:
    build: .
    container_name: damai-ticket
    network_mode: host
    env_file:
      - ./config/.env
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
      - ./screenshots:/app/screenshots
    restart: "no"
