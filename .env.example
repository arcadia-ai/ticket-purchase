# 手机IP地址（网络ADB连接时需要）
# 在手机"设置-关于手机-状态信息"中查看WiFi IP地址
DEVICE_IP=192.168.1.100

# Appium Server端口（默认4723）
APPIUM_PORT=4723

# 日志级别（可选：debug, info, warn, error）
LOG_LEVEL=debug

# 使用说明：
# 1. 复制此文件为 .env
#    cp .env.example .env
# 2. 修改 DEVICE_IP 为你的手机实际IP地址
# 3. 运行 docker-compose --profile network up -d
